import { FormatingPipePipe } from './formating-pipe.pipe';

describe('FormatingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
